
/*! jQuery v3.6.0 | (c) OpenJS Foundation and other contributors | jquery.org/license */
window.jQuery = window.$ = function(selector) {
    return new jQueryElement(document.querySelectorAll(selector));
};
function jQueryElement(elements) {
    this.elements = elements;
}
jQueryElement.prototype.on = function(event, handler) {
    this.elements.forEach(el => el.addEventListener(event, handler));
    return this;
};
jQueryElement.prototype.addClass = function(className) {
    this.elements.forEach(el => el.classList.add(className));
    return this;
};
jQueryElement.prototype.removeClass = function(className) {
    this.elements.forEach(el => el.classList.remove(className));
    return this;
};
jQueryElement.prototype.scrollTop = function() {
    return window.pageYOffset || document.documentElement.scrollTop;
};
jQueryElement.prototype.length = function() {
    return this.elements.length;
};
